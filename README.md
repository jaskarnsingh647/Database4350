# Database4350
This is a database using the GO programming language. 
-------------------------------------------------------------------------------------------------
To run this make sure all files are in the same directory and then run the following command:

Remove-Item data.db -ErrorAction SilentlyContinue

.\gradebot project-1 --dir (path to your folder) --run ".\kvstore.exe"
-------------------------------------------------------------------------------------------------
If the data.db file is not present only run the second command. Make sure you put the path in place of parenthesis
